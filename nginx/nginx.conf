server {
    listen 80;
    server_name localhost;

    location / {
        proxy_pass http://localhost:3000;  # Your Node.js app runs on port 3000 locally
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Other configuration options for your server, if needed.
}
